{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "installCommand": "apt-get update && apt-get install -y libgl1"
    },
    "deploy": {
        "startCommand": "python test.py",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
